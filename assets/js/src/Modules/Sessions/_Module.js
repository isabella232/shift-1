(function () {
    'use strict';

    var dependencies = [
        'Shift.Sessions.Controllers',
        'Shift.Sessions.Services'
    ];

    angular
        .module('Shift.Sessions', dependencies);

})();