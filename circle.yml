machine:
  php:
    version: 5.4.6
  node:
    version: v0.10.28

dependencies:
  pre:
    - composer config --global github-oauth.github.com ${COMPOSER_OAUTH_TOKEN}

test:
  override:
    - phpunit tests
    - karma start shift.conf.js
