<?php

namespace Tectonic\Shift\Library\Theme;

/**
 * The theme class acts as both a theme manager, for all required themes, as well as the interface through
 * which theme files are requested. There is one step required before any rendering can be done, for starters:
 *
 * Themes must be registered.
 *
 *   Theme::register(new SomeTheme);
 */

class Theme
{
	
}
